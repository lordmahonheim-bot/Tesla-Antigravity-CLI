{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Textual Gradient (RCA)",
  "description": "Schéma définissant le signal d'erreur sémantique généré par TextGrad/Premortem.",
  "type": "object",
  "properties": {
    "error_id": {
      "type": "string",
      "description": "Identifiant unique de l'erreur (ex: LSP-042-2026-07-24)"
    },
    "failing_step": {
      "type": "string",
      "description": "Localisation chirurgicale de l'échec (ex: Section 7.3 du SKILL.md ou Ligne 42)"
    },
    "gradient": {
      "type": "string",
      "description": "La critique sémantique exacte expliquant pourquoi l'étape a échoué"
    },
    "affected_modules": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Liste des sous-agents ou modules impactés"
    },
    "severity": {
      "type": "string",
      "enum": ["TRIVIAL", "MINOR", "MAJOR", "CRITICAL"],
      "description": "Niveau de gravité déterminant la Validation Gate"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["error_id", "failing_step", "gradient", "severity", "timestamp"]
}
