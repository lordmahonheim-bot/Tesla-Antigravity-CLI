#!/usr/bin/env bash
set -euo pipefail
root="$(git rev-parse --show-toplevel)"
script="$root/53-Vigilum-Codex-2.0-Executable-Governance/core/hooks/pre-commit/tesla-pre-commit-main.sh"
if [ -x "$script" ]; then
  exec "$script" "$@"
else
  echo "🚨 Hook bypassed: $script not found. Fail-closed."
  exit 1
fi
